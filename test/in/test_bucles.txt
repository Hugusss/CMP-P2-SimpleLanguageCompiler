// Test de Estructura de Control REPEAT
int contador
contador := 0
int limite
limite := 5

// Debe repetirse 5 veces (limite)
// contador sumará: 1+2+3+4+5 = 15? No, sumamos 10 cada vez.
// 0 -> 10 -> 20 -> 30 -> 40 -> 50
repeat limite do
    contador := contador + 10
done

// Bucle anidado
int total
total := 0
int n
n := 3
repeat n do
    total := total + 1
    // Dentro repetimos 2 veces
    repeat 2 do
        total := total + 1
    done
done
// n=3. Por cada vuelta suma 1 (externo) + 2 (interno) = 3. Total 3*3 = 9.

contador
total
